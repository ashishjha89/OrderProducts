# AWS EC2 (Single Instance) - Creation

## Expected outcome

- 1 EC2 instance
- Runs **all containers** (infra + services)
- ~4 GB RAM needed → **must fit**
- Used **on-demand**, stopped when done
- **Lowest possible cost** without fighting OOM / crashes

## Calculate CPU and memory needs

- *Run locally all docker containers and find memory requirements*.
  - I see following usage:
    - 20% / 800% CPU (there are 8 CPUs available).
    - `3.8 GB container memory`.
    - *Note*: These are for running following containers:
      - mysql, mongodb, zookeeper, broker, connect, zipkin
      - disocvery-server, api-gateway, product-service, inventory-service, order-service
    - *Note*: These are with negligible traffic, just everything running idle.
    - *Note*: `Assume OS + Docker daemon` (~400–600 MB). Also, there will be memory spent for `Page cache + JVM overhead`.
    - *Note*: `Real memory requirements ~ 4.5 GB RAM`
  - Appropriate EC2 Instance Sizes:
    - `t3.large` which has 8GB RAM. (Note t2.medium has just 4 GB RAM).

## 1️⃣ Choose the right EC2 instance (most important decision)

### ✅ Recommended instance

**`t3.large`**

| Resource         | Value          |
| ---------------- | -------------- |
| vCPU             | 2              |
| RAM              | **8 GB**       |
| Cost (eu-west-1) | ~€0.083 / hour |

### Why not smaller?

- Your containers already use **~4 GB**
- Add:
  - OS
  - Docker
  - JVM overhead
- `t3.medium (4 GB)` will **OOM eventually**
- `t3.large` gives safety + sanity

💡 **Cost reality**

- 15 minutes/day → **~€1/month**
- This is already “cheap enough” without pain

## 2️⃣ Launch EC2 (minimal setup)

### AMI

- **Amazon Linux 2023**

### Storage

- **20 GB gp3** (default is fine)
- No extra volumes
- No backups
- No snapshots needed

### Network

- Public subnet
- Auto-assign public IPv4 → **Enabled**

### Security Group (minimal)

Inbound:

- SSH (22) → your IP only
- API Gateway (8080) → your IP only
- (Optional) Zipkin (9411) → your IP

Outbound:

- Allow all

## 3️⃣ Install Docker & Docker Compose (once)

SSH into EC2:

```bash
# Update system
sudo dnf update -y

# Install Docker
sudo dnf install -y docker

# Start and enable Docker
sudo systemctl start docker
sudo systemctl enable docker

# Add ec2-user to docker group
sudo usermod -aG docker ec2-user

# Verify Docker installation
docker --version

exit
```

Reconnect (important).

Install compose plugin:

```bash
docker compose version
```

If missing:

```bash
# Create plugin directory
sudo mkdir -p /usr/local/lib/docker/cli-plugins

# Download Docker Compose V2
sudo curl -SL https://github.com/docker/compose/releases/download/v2.27.0/docker-compose-linux-x86_64 \
  -o /usr/local/lib/docker/cli-plugins/docker-compose

# Make executable
sudo chmod +x /usr/local/lib/docker/cli-plugins/docker-compose

# Create symlink (optional, for backward compatibility)
sudo ln -sf /usr/local/lib/docker/cli-plugins/docker-compose /usr/local/bin/docker-compose

# Verify installation
docker compose version
```

## 4️⃣ Prepare your compose file for EC2 (important changes)

- Use images only (no build)

## 5️⃣ Reduce memory pressure (CRITICAL)

- Add memory limits per service (in docker-compose)
- This ensures:
  - No single container can kill the host
  - JVM respects limits (you already configured JVM flags)

## 6️⃣ Pull & start (daily workflow)

### First time (or after instance start)

```bash
docker login ghcr.io
docker compose pull
docker compose up -d
```

Wait ~1–2 minutes.

Verify:

```bash
docker ps
docker stats
```

---

## 7️⃣ When done practicing (MOST IMPORTANT)

### Stop everything

```bash
docker compose down -v
```

### Stop EC2

From AWS Console:

- **Stop instance**

💸 Cost now = **€0 while stopped**

---

## 8️⃣ Optional: fastest & cleanest teardown

If you’re done for the day:

- **Terminate instance**
- Relaunch tomorrow

Because:

- No persistence needed
- Everything is in images
- This avoids:

  - Disk costs
  - Zombie resources
  - Accidental spend

## 9️⃣ Cost guardrails (do this once)

### Create a budget

- €10/month
- Alerts at 50%, 80%, 100%

### Enable cost anomaly detection

- Threshold: €3–5

## Final daily routine (simple mental model)

### Start

1. Start EC2
2. `docker compose up -d`
3. Practice

### Stop

1. `docker compose down -v`
2. Stop EC2

## TL;DR

- ✅ Use **t3.large**
- ✅ No persistence
- ✅ Images only
- ✅ Memory limits
- ✅ Stop or terminate daily
- ✅ Cost ≈ **€1–2/month**
